#!/bin/bash
# #SBATCH --account=commons
# #SBATCH --partition=commons
#SBATCH --account=ctbp-common
#SBATCH --partition=ctbp-common
#SBATCH --export=All
#SBATCH --job-name=1a
#SBATCH --time=24:00:00
#SBATCH --gres=gpu:1
#SBATCH --array=1-3

source activate openmm

export ID=$SLURM_ARRAY_TASK_ID
echo SLURM_ARRAY_TASK_ID $ID

python /scratch/mw88/NQ/array/1_N17_6/mm_run_pbc.py N17_6 --to 300k/rep$ID --platform OpenCL --steps 2e7 --tempStart 300 --tempEnd 300 --reportFrequency 15000 -f forces_setup_pbc.py > 300k/rep$ID.log

